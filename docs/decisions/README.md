# Architecture decisions

Record decisions that constrain future implementation. Keep each record small:
context, decision, consequences, and what would justify revisiting it.

The current architecture is summarized in
[`../architecture.md`](../architecture.md). Add a numbered decision record when
a change would otherwise be rediscovered or quietly reversed by a later agent.
